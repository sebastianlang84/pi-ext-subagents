{
  "name": "pi-subagents",
  "version": "1.1.0",
  "description": "Pi extension for delegating work to isolated subagents.",
  "keywords": [
    "pi-package",
    "pi",
    "subagents",
    "agents"
  ],
  "license": "MIT",
  "type": "module",
  "files": [
    "src",
    "README.md",
    "CHANGELOG.md",
    "LICENSE"
  ],
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "@earendil-works/pi-agent-core": "*",
    "@earendil-works/pi-ai": "*",
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*",
    "typebox": "*"
  },
  "devDependencies": {
    "jiti": "*"
  }
}
