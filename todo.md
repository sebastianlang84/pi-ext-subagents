# TODO — Active Backlog

Purpose: active open work only. Completed work belongs in `CHANGELOG.md`, git history, or release notes — not as checked-off TODO entries.

## P1 (Before next release)

- [ ] Choose the next release version and move `CHANGELOG.md` `Unreleased` entries into that release section before publishing/tagging.

## P2 (Later)

- [ ] Optional local install smoke: `pi install .`, restart or `/reload`, then confirm the `subagent` tool appears and can run a trivial user agent.
